import 'package:flutter/material.dart';

class ActiveAppliedView extends StatelessWidget {
  const ActiveAppliedView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      color: Colors.deepPurple,
    );
  }
}
